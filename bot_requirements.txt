# The Claimers — Telegram Bot Service (Service 2)
# Pinned versions for reproducible builds on Render

flask==3.0.3
gunicorn==22.0.0
requests==2.32.3
python-dotenv==1.0.1

# requests retry / connection pool tuning
urllib3==2.2.1
